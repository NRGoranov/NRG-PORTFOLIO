# Contact form email (same variables as GoLa / DiplomnaWork)
EMAIL_USER=
EMAIL_PASSWORD=
TO_EMAIL=nrgoranov@gmail.com

# Or explicit SMTP:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=

# Clothing wishlist admin (view subscriber emails at /clothing/wishlist)
CLOTHING_ADMIN_PASSWORD=

# Supabase (from Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only — wishlist API (Settings → API → Secret keys → default). Never expose in the browser.
SUPABASE_SECRET_KEY=
